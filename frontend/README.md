# Frontend

Запуск локально:
```bash
cd /Users/macbook/Desktop/backend/tg_my/frontend
echo 'VITE_API_BASE_URL=http://localhost:8080' > .env.local
npm i
npm run dev -- --port 5173
```

Основной код приложения находится в `src/`.
